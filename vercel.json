{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "node scripts/build-vercel-api.mjs && PORT=3000 BASE_PATH=/ pnpm --filter @workspace/maktaba build",
  "outputDirectory": "artifacts/maktaba/dist/public",
  "functions": {
    "api/[...path].mjs": {
      "maxDuration": 10
    }
  },
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/[...path].mjs" },
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
